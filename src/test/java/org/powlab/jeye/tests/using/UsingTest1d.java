package org.powlab.jeye.tests.using;

public class UsingTest1d {
// TODO java8
//    private static Stream<Path> getPathStream(Path path) throws IOException {
//        try (Stream<Path> paths = Files.walk(path)) {
//            return paths.collect(Collectors.<Path>toList()).stream();
//        }
//    }
}
